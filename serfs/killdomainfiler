z#!/bin/sh
#inkVerbSerf! verb.ink
set -e

# This removes an existing domain link from an existing ftpfiler's home ~/domains folder

# How to use:
## ./killdomainfiler [olddomain.tld] [filer username]


OLDDOMAIN=$1
FILER=$2

rm -f /srv/guru/filers/${FILER}/domains/${OLDDOMAIN}

